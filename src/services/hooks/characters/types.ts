export type CharacterProps = {
  id: number;
  name: string;
  image: string;
};

export type AcessosProps = {
  visualizar: boolean;
  inserir: boolean;
  editar: boolean;
  deletar: boolean;
  imprimir: boolean;
};
